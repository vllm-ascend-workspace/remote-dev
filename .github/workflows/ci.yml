name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contracts:
    name: Contracts (Python ${{ matrix.python }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        python: ["3.9", "3.12"]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Byte-compile every module
        run: python3 -m compileall -q .
      - name: Unit tests
        # Mocked transports and temporary state only; no SSH, no hardware.
        run: python3 -m unittest discover -s tests
      - name: Local contract gates and MCP burden report
        run: python3 tools/validate_remote_dev_scaffold.py --local-only
